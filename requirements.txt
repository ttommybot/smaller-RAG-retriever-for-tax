# RAG for Tax 项目依赖

# 核心依赖
sentence-transformers>=2.2.0    # 本地 Embedding 模型
pyyaml>=6.0                      # 配置文件加载
numpy>=1.20.0                    # 向量运算
scikit-learn>=1.0.0              # 机器学习工具
scipy>=1.7.0                     # 科学计算

# 文档加载
python-docx>=0.8.10              # 读取 Word 文档

# 异步 HTTP 依赖（问题集生成）
aiohttp>=3.8.0                   # 异步 HTTP 客户端

# Web 交互界面依赖
flask>=2.0.0                     # Web 框架
flask-cors>=3.0.0                # Flask CORS 支持

# 模型下载
huggingface_hub>=0.10.0          # 下载 HuggingFace 模型

# 安装全部依赖
sentence-transformers 
pyyaml 
numpy 
scikit-learn 
scipy 
python-docx 
aiohttp 
flask 
flask-cors 
huggingface_hub
